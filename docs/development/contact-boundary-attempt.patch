diff --git i/packages/ai/src/naval.ts w/packages/ai/src/naval.ts
index b9509fa..eeab235 100644
--- i/packages/ai/src/naval.ts
+++ w/packages/ai/src/naval.ts
@@ -38,7 +38,10 @@ const portStatus = (view: Observation, cell: number, cells: ObservedCells, sea:
   const town = view.settlements.find(town => town.factionId === view.factionId && town.cell === cell);
   const water = town ? portWaters(view, town, cells) : coastalWaters(view, cell, cells);
   if (!water.length || water.some(next => !isSea(cells.get(next)))) return 'enclosed';
-  const statuses = water.map(next => sea.basinStatus(next));
+  const statuses = water.map(next => {
+    const status = sea.basinStatus(next);
+    return status === 'unknown' && sea.boundedByLand(next) ? 'enclosed' : status;
+  });
   return statuses.every(status => status === 'open') ? 'open' : statuses.some(status => status === 'enclosed') ? 'enclosed' : 'unknown';
 };
 const portRank = { enclosed: 0, unknown: 1, open: 2 } as const;
@@ -357,10 +360,10 @@ export function planNaval(view: Observation, coinBudget: number, options: NavalP
     }
     if (passengers.length) {
       const colonizing = passengers.some(containsFounder); // Legal canFound is intentionally false while aboard.
-      // A proved, fully charted inland basin has no undiscovered sea exit. Its
-      // ferry can still found a safely separated town on the surrounding land;
-      // unknown boundaries or exhausted search budgets never authorize this fallback.
-      const enclosed = colonizing && seaKnowledge.enclosed(fleet.cell);
+      // Charted water or a closed observed land boundary can prove no sea exit.
+      // Unknown interior terrain is not revealed by the latter proof; only the
+      // absence of an escape route permits this ordinary local landing fallback.
+      const enclosed = colonizing && (seaKnowledge.enclosed(fleet.cell) || seaKnowledge.boundedByLand(fleet.cell));
       const viableLanding = (cell: number) => isPassable(cells.get(cell)?.terrain ?? 0) && !occupied.has(cell)
         && (colonizing ? !cells.get(cell)?.settlementId && nearest(cell, view.settlements) >= settlementSpacing(view, cell) : nearest(cell, towns) >= 4)
         // Do not unload at the first free beach on the departure landmass. A cell outside
diff --git i/packages/ai/src/sea-knowledge.ts w/packages/ai/src/sea-knowledge.ts
index c57eced..b6c3cac 100644
--- i/packages/ai/src/sea-knowledge.ts
+++ w/packages/ai/src/sea-knowledge.ts
@@ -2,6 +2,7 @@ import { neighbors, TERRAIN } from '@theandril/mapgen';
 import type { Observation } from '@theandril/sim';
 
 export const MAX_WATER_KNOWLEDGE_NODES = 8192;
+export const MAX_SEA_BOUNDARY_NODES = 8192;
 export type BasinStatus = 'open' | 'enclosed' | 'unknown';
 export type BasinRelation = 'connected' | 'separate' | 'unknown';
 
@@ -67,8 +68,40 @@ export function createSeaKnowledge(view: Pick<Observation, 'cells' | 'width' | '
     if (result !== 'unknown') return result;
     inspect(b, connectedTo(a)); return resolve();
   };
+  // A closed observed land boundary proves that even an all-water completion
+  // of its unknown interior has no sea exit. It proves neither the terrain in
+  // that interior nor a navigable connection between two observed water cells.
+  // Keep this separate from the historical charting/connectivity proof above.
+  const boundaries = new Map<number, boolean>();
+  let boundaryExpandedNodes = 0;
+  const boundedByLand = (origin: number): boolean => {
+    if (cells.get(origin)?.terrain !== TERRAIN.water) return false;
+    const cached = boundaries.get(origin); if (cached !== undefined) return cached;
+    if (boundaryExpandedNodes >= MAX_SEA_BOUNDARY_NODES) return false;
+    const queue = [origin], visited = new Set(queue);
+    let enclosed = true;
+    search: for (let cursor = 0; cursor < queue.length; cursor++) {
+      if (boundaryExpandedNodes >= MAX_SEA_BOUNDARY_NODES) { enclosed = false; break; }
+      const cell = queue[cursor]!, x = cell % view.width, y = Math.floor(cell / view.width);
+      boundaryExpandedNodes++;
+      if (x === 0 || y === 0 || x === view.width - 1 || y === view.height - 1) { enclosed = false; break; }
+      for (const next of neighbors(cell, view.width, view.height)) {
+        if (visited.has(next)) continue;
+        const terrain = cells.get(next)?.terrain;
+        if (terrain !== undefined && terrain !== TERRAIN.water) continue;
+        const proof = boundaries.get(next);
+        if (proof !== undefined) { enclosed = proof; break search; }
+        if (visited.size >= MAX_SEA_BOUNDARY_NODES) { enclosed = false; break search; }
+        visited.add(next); queue.push(next);
+      }
+    }
+    for (const cell of visited) boundaries.set(cell, enclosed);
+    return enclosed;
+  };
   return {
     get expandedNodes() { return expandedNodes; },
+    get boundaryExpandedNodes() { return boundaryExpandedNodes; },
+    boundedByLand,
     nodeBudget,
     basinStatus(origin: number): BasinStatus { const component = inspect(origin, component => component.edge); return component?.edge ? 'open' : component && complete(component) ? 'enclosed' : 'unknown'; },
     basinRelation: relation,
