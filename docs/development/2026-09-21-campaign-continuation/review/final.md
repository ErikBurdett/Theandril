# Final factual and visual review — campaign continuation

**Verdict: approved as an accurate local development checkpoint and unpublished
draft. No remaining factual or visual blocker in the reviewed packet. M0 is not
complete: the default full suite still fails its Epic timing requirement.**

Reviewed on 2026-09-21 after the parent froze status, roadmap, evidence and draft
claims and confirmed the final Pages process exited successfully. This review
read retained results, inspected images and compared file hashes; it did not
rerun tests, builds, benchmarks or browser journeys. The only file written by
this review is this report. Adding links to this verdict and the parent prompt
report does not turn it into publication or release approval.

## Source and verification integrity

All **21 files** in `source-manifest.json` match their recorded SHA-256 values,
including the four implemented optimization groups, unchanged timing/contact
tests and unchanged test/CI configuration. The manifest itself hashes to
`61b2546fe968d3a8e92844e82444147ed70ae3c9616c0705fd70d4e5f4f372c5`.
The source hashes agree with the separate bounded reviews for
[explored-cell sorting](canonical-cells.md), [observation publication](observations.md),
[technical formatting](pretty-json.md) and [movement previews](movement-preview.md).
The earlier roadmap-start checkpoint and its source manifest remain historical
evidence; they are not substituted for current verification.

All **eight retained log hashes** in `verification.json` match their files, and
its source-manifest digest matches. The raw logs support these current results:

| Check | Reviewed result |
| --- | --- |
| Typecheck, lint, content and existing-art validation | Pass; content hash remains `b79c78ed`. |
| Production Pages build | Pass, with the existing annotation and bundle-size warnings retained. |
| Default full headless suite | **1,818/1,819 tests; 218/219 files; 90.01 seconds overall. Failed.** |
| Sole full-suite failure | Epic archive verification: **64.312 seconds against 60 seconds**. |
| Affected Chromium gameplay | **25/25**, 2.5 minutes. |
| Complete production Pages Chromium suite | **27/27**, 33.2 seconds. |

The preceding 65.905-second full-suite failure and 50.45-second isolated diagnostic
are retained and correctly distinguished. The isolated pass is not presented as
default-suite recovery. Overlapping scoped results are not added into a larger
test total. The browser claims are scoped Chromium coverage; they do not assert
all gameplay scenarios, other browsers, hosted CI or release-scale certification.
The current timing test, contact/pacing tests and test/CI configuration have no
working-tree diff. No source change in this packet relaxes those gates.

## Claims, roadmap and experiment disposition

The implementation-status top section, M0 checklist, continuation README and
developer packet agree on four bounded processing improvements, unchanged
rules/save version **17**, unchanged content, and the outstanding timing failure.
The new preview API preserves the same range search and shared 4,096-node budget;
the retained eight-turn voyage preserves 22 accepted plan commands and final hash
`fa29672f`. These are compatibility and processing claims, not new game systems.

The existing canonical roadmap remains the sole plan. M0 stays in progress,
client-state diplomacy and unification stay pending, and all fifteen whole release
gates stay open. The genuine v17 diplomacy archive is correctly labeled
compatibility preparation with authored initial contact, ordinary paid peace and
a pending offer. It is not evidence of implemented client contracts or a second
victory.

The encoded save/hash experiment is explicitly **rejected**, with its failed
design review and mutation regressions retained. A search of `packages`, `apps`
and `scripts` found no imports or use of its experimental projection/composition
helpers. Production `save.ts` still matches the separately approved non-caching
source hash. Ordinary-data benchmark equality is not described as resolution of
the rejected hook/memory issues. The README now correctly reports the observed
18–45% regression range across the four changing-land cases.

The public catalogue still uses the historical source pin
`fcae402da6c290b93a9a6933510c74538477ad04`; new local evidence is separate. The
draft slug is absent from the public dispatch catalogue. No commit, publication,
deployment or hosted CI recovery is claimed, and publication remains unauthorized.

## Current image inspection

I opened all three retained current PNGs and inspected their actual pixels. Each
matches its original Playwright capture byte-for-byte, with matching SHA-256 and
PNG dimensions. Updated provenance hashes to
`ef7e0d5e2749cb70295bd79f6bbfe0f00d22387de8c811d44d2f1d5e12387a62`.

| Image | Actual inspection |
| --- | --- |
| `technical-ledger.png`, 1440 × 1000 | Real campaign chronicle dialog, accepted command JSON, turn selector and complete technical download control are visible and readable. Seed 20260905/Short watch mode is visible; the selected technical turn is 41 in a campaign that concludes on turn 42. |
| `history-390.png`, 374 × 828 | Real history dialog captured as an element inside a 390-pixel viewport. The complete-record text states victory on turn 42. Chapter selection, previous/next controls, founded-hearth prose and download control fit the narrow modal. |
| `land-quotes-390.png`, 390 × 844 | Current land quotes in the actual game show an enabled Terraced fields option and disabled alternatives with reasons. The test captures this after saved work was restored and completed, then another tile selected; the corrected provenance describes that stage. |

The draft's two current images are the ledger and history captures above. Their
captions agree with the generated-start watch test, including saved continuation
and the actual victory turn. They illustrate existing UI output and export
readability, not the Epic gate or new art. The three older images in the same
packet are explicitly labeled prior-checkpoint imagery; their earlier independent
visual review is preserved rather than claimed as fresh current-source coverage.

## Review corrections and remaining limit

Before this verdict, the parent corrected the broad cache regression range,
clarified that the synthetic formatting corpus repeats archive records containing
real battle/event data, and aligned the land-image caption with its actual capture
stage. Browser rows now contain final passing results instead of pending language.
These were documentation precision corrections, not production defects.

The reviewed documents pass `git diff --check`. No additional corrective edit is
requested. The next acceptance work remains the failed default-suite Epic timing
case; neither these bounded approvals nor the passing browser journeys close M0
or any whole release gate.
