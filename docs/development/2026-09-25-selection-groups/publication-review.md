# Saved selection groups: publication review

Factual review began 25 September 2026; final publication review resumed
26 September 2026. Implementation pin:
**`0d26fa3c34ac89164637f515e95671420400a841`**.

**Status: factual/editorial, image-provenance and final built-production layout
review passed.** No blocking correction is required in the frozen edition 09
candidate. Deployment and live readback remain separate parent checks.

## Independence and scope

This reviewer did not author the article, current-roadmap changes, group UI,
canonical implementation or persistence changes. The reviewer authored the new
browser journeys, inspected their gameplay captures and organized those artifacts;
checking their recorded results here is evidence reconciliation, not independent
browser execution. The parent ran those journeys. The separately linked
integration review discloses its UI authorship and independently reviewed
canonical/worker boundaries. This task edits only this publication report and
retains exact publication captures with provenance. It runs no browser, full
suite or benchmark.

## Facts and evidence

The entry `selection-groups`, sequence **9**, titled **Recall a group, then give
the order**, and the current roadmap use the complete implementation pin above.
All **34 distinct article/current-library evidence paths** exist at that pin;
none cites the later publication work as implementation proof.

The final article subtitle and current-roadmap snapshot are dated **26 September
2026**, the publication date. The immutable implementation evidence and original
gameplay captures remain dated **25 September**. This editorial date correction
does not rewrite the prior dispatches or imply that their tests were rerun.

The player-facing account matches the committed source: twenty-four groups per
realm across both kinds, at most 128 members, forty-character names unique within
each kind ignoring case, separate choosing and recalling, and explicit ordinary
postings/charters. Group edits are canonical recorded commands with a dedicated
counter; Recall changes only the active tab's checks. Permanent losses prune
membership, carried land armies remain saved but are skipped for orders, empty
groups survive, and Update replaces the full membership. Saved selection groups
travel with the campaign; personal charter policy templates remain browser-local.

Rules/save32 and unchanged content `015468d1` are accurately stated. The article
preserves the four genuine rules31 checkpoints, exact historical bytes/replay,
fresh migration registers, strict historical rejection and mixed-history
continuation boundaries. It also retains the reviewed migration-array and
malformed-response findings and their corrections.

The sixty-four-realm research-save defect and repair match the committed failing
regression, implementation and **35 focused passing tests across three files**.
Both generated compositions—64 major realms, and 40 major realms plus 24
city-states—roundtrip. Sixty-five is rejected. The historical research schemas
remain frozen and historical export refuses an unrepresentable state. The article
describes this as repair of existing supported scope, not a new accepted feature
or closed save/scale gate.

The pinned logs record **1,941 headless tests across 243 files in 59.19 seconds**,
**20 affected Chromium gameplay journeys in 2.6 minutes**, and **one separate
built-production journey in 7.8 seconds**. These scopes overlap and remain
separate. The article distinguishes authored mature-realm evidence from generated
production play and retains both premature-generation browser-test failures.
Later publication checks do not replace these pinned counts. Earlier Standard
234 / Long 342 / Epic 379 measurements remain explicitly historical, with no new
pacing measurement or campaign-resolution/AI-policy change claimed.

Benchmark numbers were checked against the committed JSON. Huge/Legendary cover
48/64 authored realms and 147,456/196,608 member references. Three warmups precede
fifteen isolated samples. Retained medians are 3.234/4.202 ms; loss medians are
3.274/4.205 ms, with loss p95 values 4.136/4.910 ms. Group metadata adds
36,518/36,590 bytes to full observation JSON of 643,105/642,803 bytes. Review
corrected an initial phrase calling the added-byte delta the standalone metadata
size; the final wording states the increase accurately. The synthetic scope,
strict roundtrip and exclusions of full turns, packed worker responses,
rendering, sustained memory and organic growth are explicit.

## Exact image and historical preservation

The reviewer opened the final public PNG. It equals the original image at the
implementation pin, byte for byte: **318 × 121**, **15,348 bytes**, SHA256
**`28236174d7b432ccf6ed1188b9529fe9a5f41d8b45ca61a7c59358a58f42e2ec`**.
The original is a direct browser clip at **x36 / y373 / width318 / height121**
within a 390 × 844 viewport. The final caption correctly identifies the authored
Legendary/gen4/seed20260905 fixture, Watch company recalled before any posting,
and the count/status/outer focus pixels excluded from the clip. The manifest
distinguishes capture-time clipping from absent post-capture transformations.
The full gameplay captures and their explicit context remain linked.

All **eleven source hashes** and **five retained artifact hashes** in the pinned
gameplay provenance match their Git blobs. The new public copy has no post-capture
crop, resize, re-encoding or generated pixels. Frontend and public manifests are
byte-identical. The prior **thirteen asset records and image files** are unchanged.
The entire historical article suffix is unchanged, preserving **all eight prior
dispatches** and their individual source pins.

The fourteen public images total **3,134,122 bytes**, leaving **11,606 bytes** under
the unchanged **3 MiB** budget. The previously stale top-level derived total is
now correct, and its test compares the manifest total with actual file bytes.
No budget or historical evidence was relaxed.

## Current roadmap and frozen candidate

The current snapshot advances to rules32 and the implementation pin. Delivered
named selections replace the stale remaining named-group claim; theaters,
patrol/escort roles, army-order templates, production sequences, broader governors
and combined mature-realm acceptance remain open. Historical dated descriptions
retain their original claims.

The **23 roadmap items** remain **6 completed checkpoints, 13 in progress and 4
pending**. They contain **59 delivered and 49 remaining editorial bullets**;
those are unweighted text entries, not completion percentages. **All fifteen
release gates remain open: 13 in progress and 2 pending.** M3 remains in progress.

The author reports **24 focused publication tests across four files**, scoped
lint and whitespace checks passing. The parent subsequently recorded the final
checks below. Frozen candidate SHA256 values, recomputed after displayed-pixel
review and unchanged from the final date correction:

| File | SHA256 |
| --- | --- |
| `apps/web/src/updates/content.ts` | `38e0b4b151f00d90fea185a364b73d3074cf51d65e55dbac9f213fce2061ba10` |
| `apps/web/src/updates/library.ts` | `925327c31f39b1303c3dd666576966eb67dd6e0d0d77f7bbf2460e16472279aa` |
| `apps/web/src/updates/media.json` | `20ca2673a735c1be3678517340c3956cfc231b0bbd87ef2dd2550321f006b34e` |
| `apps/web/public/updates/provenance.json` | `20ca2673a735c1be3678517340c3956cfc231b0bbd87ef2dd2550321f006b34e` |
| `apps/web/src/updates/journal.css` | `133a74e9d88c9b07522533b0409d36b01802f33515e45092eda4b0ef92c62234` |

## Final displayed-layout review

The reviewer opened all twelve retained journal PNGs: reader heading, evidence
chapter and complete illustration/caption at **1440 × 1000 / 100% text**,
**1366 × 768 / 100% text**, **390 × 844 / 100% text** and
**390 × 844 / 130% text**. The heading and subtitle display **26 September 2026**;
the implementation counts in the evidence chapter remain the historical pinned
counts. Navigation wraps without collision, the title and prose remain within
the paper, and the enlarged text retains readable line breaks. The compact
illustration, Inspect image control, caption and Original image link all remain
visible within their element bounds. The caption explains the authored fixture
and exact native gameplay clip; the narrow layouts do not imply an organically
grown campaign or present the image as interactive game controls.

All four retained `layout-evidence.json` files report the decoded 318-pixel-wide
image from the actual `/Theandril/updates/` path, no page/HTTP errors and no
simulation workers on the publication page. The passing journeys also assert
no horizontal overflow. Text scaling changes the root font size, not browser
zoom; the screenshot records do not claim certification at other zoom levels.

The reviewer also opened all six roadmap PNGs: introduction, keyboard-opened
evidence item and pending systems at **1440 × 1000 / 100% text** and
**390 × 844 / 130% text**. The current introduction displays the corrected
26 September snapshot, rules32, the short source pin, and **6 / 13 / 4** item
counts with the explicit warning that these are not a completion percentage.
The evidence disclosure, gate links and remaining-acceptance text wrap within
the narrow column. Pending systems remain visibly pending. The long narrow
evidence capture is a native full-element screenshot, not a scaled viewport or
an edited composite. Keyboard disclosure and skip navigation, and horizontal
overflow checks, passed in the recorded journeys.

Exact copies are retained under [publication-screenshots](publication-screenshots/):
**18 PNGs and four layout JSON records**, **4,813,369 bytes** before their manifest.
Their [provenance](publication-screenshots/provenance.json) records each original
output path, SHA256, dimensions, viewport/text settings, capture method and the
final source hashes. No image was cropped, resized, re-encoded or otherwise
transformed after capture. The larger raw run remains the parent's artifact
management responsibility; this review retains the relevant reader and roadmap
surfaces without copying the older dispatch galleries.

## Publication checks and verdict

The reviewer read the parent's retained logs. The later publication suite passed
**1,943 tests across 243 files in 55.54 seconds**
([publication-tests.log](publication-tests.log)). After the date-only correction,
the final focused run passed **24 tests across four files in 208 ms**
([publication-final-date-tests.log](publication-final-date-tests.log)). Its first
sandbox attempt is preserved separately in
[publication-final-date-sandbox.log](publication-final-date-sandbox.log): the
source-pin test could not spawn Git (`EPERM`); this was an execution restriction,
not a failed product assertion. The successful retry retains the assertion.

The final typecheck and lint logs are empty successful records, and the
[Pages build](publication-build.log) passed. The actual local production Pages
run passed **31 journeys in 54.5 seconds**
([publication-pages.log](publication-pages.log)), including the four reader
layouts, roadmap behavior and separate ordinary-control production group
journey. These later, overlapping publication checks remain distinct from the
article's immutable **1,941 / 20 / 1** implementation evidence.

No factual, provenance or displayed-layout blocker remains for publishing this
edition09 candidate. This bounded verdict does not certify untested browsers,
sustained campaign scale, live deployment or overall release acceptance. M3 and
all fifteen release gates remain open.
