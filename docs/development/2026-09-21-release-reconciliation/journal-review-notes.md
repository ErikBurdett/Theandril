# Campaign foundation journal publication handoff

The new `campaign-foundation-and-development-order` entry, current scope/reference shelf and roadmap use implementation checkpoint `1e41ec24965e46e8035c57b4f54632a690b8b712`. The original three published stories and four image pins remain unchanged. The roadmap retains its 23 stable item IDs, six bounded completed checkpoints and all fifteen open whole release gates. The original naval findings move to delivered behavior; M0 retains the Epic timing and integrated acceptance work.

The story labels the 21 September local executions explicitly: 1,857/1,858 headless, Epic 64.779 seconds against 60, scoped 25/25 Chromium gameplay and 27/27 production Pages. These are evidence at the implementation checkpoint, not new deployment verification. It states that rules/save remain 17, content remains `b79c78ed`, client contracts/unification are not implemented, and neither a rejected experiment nor a worker cap was adopted. No gameplay scope is added or cut.

The two published PNGs are byte-for-byte copies of the independently reviewed checkpoint screenshots. The author inspected both originals before publication preparation. Technical ledger: 1440 × 1000, SHA-256 `bf6c0c3924d3aa25ab8f2c186e0aed97a852489355eb62f805067beeda3cb799`. Transport landing: 390 × 844, SHA-256 `7dea9b20985e5f917910eb632281b2b796d11c7526c9d3f929e19c88f644f516`. There is no transform or re-encoding. Captions distinguish the generated Short campaign from the authored human-command naval regression. All six journal images total 870,767 bytes under the existing 3 MiB limit. The narrow article image is capped to its native width.

The author ran the focused journal suite on Node 22.23.2: **44/44 tests in 16 files**, 13.95 seconds. A preceding sandbox attempt failed five Git subprocess checks with `spawnSync git EPERM`; the authorized retry passed without changing tests. Two rendered current-reference-link assertions were then added to the existing journal scenario; that file passed **3/3**. These checks overlap, so they are not combined. `git diff --check` and Python syntax parsing of the image reproduction script pass. The script now preserves both exact PNG copies alongside its four historical derivatives; it was not used to regenerate the historical images in this publication pass.

Parent integration owns typecheck/lint, production build, Pages browser execution, independent factual/code/displayed-pixel review, commits and live deployment readback. The new browser journeys exercise the current featured story at four viewport/text settings, current image loading, permanent links and an explicit return to the unchanged historical R17 evidence. Author inspection and unit checks do not substitute for that final production review. No deployment or overall release acceptance is asserted by this handoff.
