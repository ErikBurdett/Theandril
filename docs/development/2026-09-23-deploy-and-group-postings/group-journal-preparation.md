# Group postings journal preparation

Prepared edition **06**, `group-postings`, for the user's authorized publication on 2026-09-23. The article and current roadmap pin implementation revision **`3ae1581089411a76ecfd08a8f5f811258c4f77f6`**. Rules/save remain **31** and content remains **`015468d1`**. This is the author's preparation record; independent factual review is recorded separately in [group-publication-review.md](group-publication-review.md). The parent owns final publication checks and deployment.

The article explains selection across search and 25-row pages, the 128-army request bound, Hold/Join destinations, individual override, group clearing, canonical refusals and interrupted-recording recovery. It separates the authored Legendary browser fixture from the worker-harness transfer sample. It advances ACT-32 without completing M3, and preserves every open release gate. The current ledger and existing `empire-management` roadmap item record the bounded delivered behavior and retain theaters, patrol/escort roles, durable groups, templates and mature-campaign acceptance as remaining work.

The pinned evidence remains **1,876 headless tests across 234 files**, **seven affected Chromium journeys** and **27 production Pages checks**. Those checks preceded this article. The later production-only group-order scenario and the parent's final publication checks are deliberately not claimed at the implementation pin. The article does not add overlapping execution totals or rerun pacing for this UI/transport change.

## Focused verification

```text
./node_modules/.bin/vitest run apps/web/src/updates/content.test.ts apps/web/src/updates/media.test.ts apps/web/src/updates/roadmap.test.ts apps/web/src/updates/validation.test.ts apps/web/src/updates/journal.test.ts
Test Files  5 passed (5)
Tests       23 passed (23)
Start at    14:45:37
Duration    277ms
```

The command ran with approved access to its Git subprocess checks. Scoped ESLint and `git diff --check` pass. Immutable-source assertions resolve each article, current reference and image source at its declared revision. A separate byte comparison confirms all **five preceding dispatch objects** remain unchanged, including fleet edition 05's `b623c2c` source pin. The existing nine media records are preserved.

`tests/gameplay/home.spec.ts` and `updates.spec.ts` now expect six dispatches and feature edition 06. The reader journeys load and capture both new illustrations at their existing desktop, narrow and 130% text settings. They also follow edition 05 and assert its historical source link remains pinned. No development server was started for this publication preparation; the parent runs the production Pages suite.

## Exact image publication

Both public PNGs are untransformed copies of the reviewed sources under this directory's `screenshots/`. The authored Legendary fixture uses seed **20260905**, 100 owned armies, forty owned hearths and 4,000 total armies. It then exercises ordinary controls. The desktop image shows 100 accepted orders and cleared selection; the narrow image shows the group selected before clearing after an individual override. Captions disclose the authored setup. Both images were inspected directly during preparation and independently during factual review.

| Image | Viewport | Bytes | SHA256 |
| --- | --- | ---: | --- |
| `group-postings-desktop.png` | 1440 × 1000 | 616,230 | `d0e3a66ceee2c4d0767b9b9e12711215bd447a5fa05aaaeb9bd942843e2de8f5` |
| `group-postings-narrow.png` | 390 × 844 | 140,060 | `4f5410a3143b8c6fbfb099a79210db4cfe807247ed2f2fd790be8bf52f78087d` |

Public/source byte equality and hashes pass. There is no crop, resize, re-encoding or generated art. Frontend media metadata and the downloadable provenance remain byte-identical. Total journal imagery is **2,912,749 bytes**, below the existing **3 MiB** budget. The narrow image reuses the existing 390px article display constraint.

## Reviewed candidate and remaining publication work

| File | SHA256 |
| --- | --- |
| `apps/web/src/updates/content.ts` | `93eb7b371648caf43169ec4f3f3a0d3bd9ba2f6649e2793fdfd26e37f6b55721` |
| `apps/web/src/updates/library.ts` | `2dd8874296bbe5a3c31cae80e77fd20684efa611590d0ccd3a01b9cc1785f0d6` |
| `apps/web/src/updates/media.json` | `1fd28d82d249468fddf9189f8a6809fef800d92cda17fb617ffab860abe23364` |
| `tests/gameplay/home.spec.ts` | `11e238f359f1581f4975173e9975f007893e26eb5061ded5f2ed7d3b4c039aad` |
| `tests/gameplay/updates.spec.ts` | `c727e758aa7d630e9685c777c70ac4dcda5a11ab69c2afad138885cc803fec02` |

Independent factual/media review found no material correction required. Parent final build, production-subpath layout/reader journeys, exact publication commit and public readback remain separate from these preparation checks. Publication does not close a release gate.
