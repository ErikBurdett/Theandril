# Slice 27 verification

Locally implemented and verified. This record covers uncapped economic growth, resource production and contracts, branching development, individual battle rules/sprites and the political overview. The existing Standard/four-seat contact failure and large-battle software-rendering limits remain explicit below. This is not Theandril 1.0 or long-campaign balance signoff.

## Canonical compatibility

Before rules changes, `scripts/capture-development16.ts` captured ten genuine rules-15 origin, paid construction, recruitment, population-limit and field-battle states with exact source hashes. The retained fixture is `packages/chronicle/src/fixtures/v15-development-baseline.json`. Tests verify old bytes/checkpoints and explicitly continue those campaigns under rules 16. Earlier generator and battle fixtures remain unchanged.

The old-claim visibility regression preserves a departed scout's unexplored outer ring through old load, modern save/load, inspection and failed commands; the first successful modern action explicitly upgrades sight. Generated old-world continuation also verifies canonical development dictionary ordering after settlement creation order differs from load order.

## Measured headless workload

[Raw kernel measurements](0039-development-kernel.json) use seven warm deterministic runs. Resource-layer generation excludes physical geography: seed 17 has 38 deposits on Tiny, 1,170 on Standard and 2,259 on Huge, with all eight resource types in each sampled world. Median resource-layer time is 0.22/3.34/6.89 ms respectively.

A synthetic 20-versus-20 deployment uses actual content stats for 2,200 soldiers. Five rounds resolve in a 3.11 ms median with exact result/seed equality and no mutation of input. This measures the headless kernel, without campaign officers, automatic abilities, rendering or browser transfer. It is not a frame-rate measurement.

## Integration checkpoints

The first full rules-16 checkpoint exposed obsolete historical-origin fixtures, resource/development dictionary ordering, old exact-price expectations and real AI spending conflicts. These were corrected without changing captured old seals or disabling assertions. The first five new browser flows passed all three development cases and found two real resource/overview UI defects: missing live resource-art bindings and an unreachable overview menu. Their rejected screenshots and the corrected generated-deposit, paid-grange, market and political-map captures are retained in the [resource/development review](../art/reviews/slice27/RESOURCE_DEVELOPMENT_REVIEW.md).

The corrected resource/development group passes **5/5 in 26.4 s**, including exact market proceeds and save/reload, earned company training, paid hearth/faction choices, 390px layouts and fog-preserving faction filters. [Browser output](0039-resource-development-browser.txt). The first complete individual-soldier functional/render group passes **7/7 in 1.4 min**; it verifies all ten land and three naval roles, real participant/casualty IDs, playback and the large-army render workload. A subsequent visual review required a tactical inspection camera because whole-field minification lost individual detail on narrow screens. Its final browser evidence is recorded separately below.

Uncapped capture, siege, diplomacy, save and historical compatibility tests pass **84/84**. Real assaults sack/raze a 100-household hearth and preserve wallets above one billion; historical pending choices retain their original pricing. [Focused output](0039-large-capture-and-compatibility.txt).

Whole typecheck, lint and content validation pass. The final build after player-observation, land-page and paging-copy corrections takes **3.462 s** (Vite 2.87 s), with main JavaScript **995.65 kB / 304.04 kB gzip**. The existing greater-than-500-kB chunk warning remains. These are bundler sizes and one local build sample, not loading-time percentiles. [Typecheck](0039-typecheck.txt), [lint](0039-lint.txt), [content](0039-content.txt), [build](0039-build.txt).

Final full headless after all read-model, fixture and page-size changes is **1,603/1,604 across 184 files in 99.79 s**, with four workers and zero skips. Every current growth, resource, development, individual-battle, art, save and historical-replay case passes. The sole failure is the previously documented Standard/four-seat contact scenario: two of four factions make contact within its unchanged 100-turn gate. This remains an AI navigation limitation, not a waived release check. [Complete output](0039-headless-final.txt). The earlier 1,599/1,600 checkpoint remains separately retained as [pre-read-model-fix output](0039-headless-before-read-model-fixes.txt).

The new Standard/24-seat and Huge/32-seat regressions are fixed: all factions make contact within their unchanged 60-turn gates, with real legal commands and exact save mirrors. The fixes protect actual caravan and first ocean-scout prices, reserve an isolated harbor's ocean-research goal before optional land research, and wait for breached walls before ordinary-strength assaults. Candidate changes that only redirected reconnaissance without improving contact were discarded. [AI causal audit](0039-growth-ai-audit.md).

The final rebuilt production bundle passes **4/4 in 12.1 s**, using actual worker/assets, paid construction with save/load, manual battle abilities and exact restored outcomes, plus spectator fog. These tests have no development mutation/inspection hooks. [Final production output](0039-production-final.txt). Earlier builds' separate 4/4 results remain in [the initial production checkpoint](0039-production.txt) and [the pre-copy checkpoint](0039-production-before-page-copy.txt).

Tactical camera and battlefield follow-up passes **5/5 in 36.5 s**. The 390px scenario selects an actual company, focuses its 60 soldiers at 4×, pans, picks through the inverse camera transform and returns to Fit while preserving the paused campaign hash and animation frames. Native soldiers are inspectable at the closer scale; numeric labels are softened by magnification and full-field ranks remain dense. Both the art agent and integrating agent inspected the retained [close-up](../art/reviews/battle-units/runtime/camera/soldiers-focused-4x-390.png) and [controls](../art/reviews/battle-units/runtime/camera/battle-inspection-controls-390.png). [Source/clip/runtime review](../art/reviews/BATTLE_UNIT_ANIMATION.md) and [hash manifest](../art/reviews/battle-units/runtime/manifest.json).

## Battlefield rendering

The isolated pre-camera render run samples 120 warmed frames after a real Brace order in an authored, funded veteran deployment: 40 real formations, six officers and **2,240 individual soldiers**. Eighteen sampled frames contain active presentation; effects peak at 12, soldier allocation at 2,240 and formation/officer labels at 46. Chromium uses **WebGL through ANGLE/SwiftShader**, not hardware GPU evidence. Frame times are **50.0 ms p50 / 66.7 ms p95**; renderer CPU is **0.7 / 8.8 ms**. Thus this workload does not achieve 60 FPS. Map-plus-battle atlas residency is 53 MiB before other caches/allocations. [Exact focused sample](0039-battle-render-focused.json).

The [full-browser Fit-camera sample](0039-battle-render.json) repeats **50.0 / 66.7 ms** frame times, with CPU **0.7 / 8.7 ms** and a 66.8 ms maximum frame. The actual 2,240-soldier allocation, 46 labels, 12 effects, 18 active samples, unchanged gameplay hashes and SwiftShader context agree with the earlier focused workload. This confirms functional bounds under the added camera; it does not establish a hardware-GPU frame rate. A final documentation audit verified 411 retained battle/hearth source and runtime references, their recorded hashes, and all sixteen resource/works approval and frame hashes.

## Full browser checkpoint and corrections

The first full Chromium invocation passes **141/149 in 13.6 minutes**. Its original output and failure artifacts remain retained: [full checkpoint](0039-browser-checkpoint.txt), `test-results/slice27-full`. The eight failures exposed two inefficient read paths and six outdated fixture/expectation assumptions:

- Human observations included unused AI development candidates. Player worker publication now requests `developmentCandidates: false`; AI retains its bounded candidate rotation, and player faction/selected-subject trees remain available. The exact Huge/32-seat startup observation drops from **24,503 to 18,275 bytes**, removing 6,228 unused bytes. The existing 20,000-byte browser budget is unchanged. Focused query/development/transfer coverage passes **74/74** in [the observation checks](0039-player-observation-tests.txt).
- Selecting another tile already in the exact ready land page requested duplicate quote pages. The hook now reuses that page only for the same town, campaign hash, registry epoch and page settings, while changed state still invalidates it. Sixteen focused query tests pass.
- The legacy-geography Huge rendering fixture now transfers packed-cell version 3: **12 bytes per row**, plus bounded sparse metadata. Its old version-1 expectation was nine bytes per row. The updated assertion directly budgets row width plus 64 KiB metadata; total transfer remains below the existing 3 MiB limit, and every cache, pool, atlas and geometry assertion remains.
- Character-roadmap coverage now includes all eight officer nodes and pays the advanced prerequisite's actual experience cost. The current-rules victory fixture reaches turn 42; archive, download, replay, exact-hash and timing assertions remain intact.
- Ordinary-strength AI siege now waits for breached defenses. The defender browser scenario completes three siege rounds and tests the actual interrupted assault at turn 4 before withdrawal/capture.
- Both synthetic empire tests exhausted their old shared treasury halfway through 32/40-town land development. A finite authored purse per town now funds every real paid claim, with exact quote deductions and all 37 claims required per fixture town. Nine focused fixture/conquest tests pass; campaign growth prices are unchanged.

A single frozen-source browser follow-up passes **22/23 in 2.0 minutes**, covering all eight original failures and the affected resource/development flows. [Exact follow-up output](0039-browser-followup.txt). The funded 32-town fixture now reaches its deeper selected-detail assertion: a request limited to 64 cells returns the selected final 45 cells and transfers **335,098 bytes**, exceeding the unchanged 200,000-byte budget after adding eight extraction-building quotes per cell. The exact one-query assertion and all 37 paid claims pass.

The [complete page-size diagnostic](0039-land-page-budget.json) checks every page, including its response wrapper. A 32-cell request appears sufficient when the selected tile lands on the short final page (13 cells / 97,702 bytes), but its full pages reach **238,407 bytes**. Player pages now request **24 cells**; all full pages of this 109-candidate fixture transfer **178,666–179,363 bytes**, and the final 13-cell page transfers 97,702. Canonical query maximum 64, territory growth and quote eligibility are unchanged.

The final land/resource/development browser batch passes **9/9 in 39.5 s**. It verifies all 109 available cells across every page, direct selection of a later-page tile, previous/next boundaries, exactly one query per new page, reuse for loaded tile clicks, each actual page below 200,000 bytes and an unchanged campaign hash. Paid extraction/market/save flows, earned development, the 100-army subject directory, narrow layouts and political settings surviving a real battle also pass. [Final browser output](0039-land-browser-final.txt).

All **149** current gameplay scenarios therefore have passing evidence across the **141/149** full checkpoint, **22/23** follow-up and **9/9** final correction. This is not a single 149/149 invocation. Prior failures, fixture corrections and measured limits remain retained rather than being replaced by a green-only report. The final functional browser and production checks used separate local ports and make no frame-rate claims; dedicated map/battle samples above were measured separately.

The final pagination review also corrects a misleading empty-detail message: a selected owned tile can be on another page, so absence from this page no longer claims it is outside the hearth's land. This wording-only change follows the full headless run and passes **20/20** focused component/query tests and scoped lint; it changes no state, query or command behavior. The final evidence case passes **1/1 in 7.9 s** in [its separate output](0039-land-browser-evidence.txt), followed by the final rebuilt production checks above. The [actual browser page measurements](0039-land-browser-transfer.json) are written explicitly to a durable file, alongside reviewed first/final-page screenshots in the [resource/development review](../art/reviews/slice27/RESOURCE_DEVELOPMENT_REVIEW.md). The integrating agent independently reviewed the corrected first-page screenshot. The rejected pre-copy and clipped captures remain retained.

The follow-up's [matched Huge map measurements](0039-art-render.json) retain 196,608 explored cells, **2,359,519 packed-cell bytes / 2,573,092 total transfer bytes**, no land/development queries and 17 MiB map-atlas residency. Camera movement reaches 18 cached chunks / 72 MiB estimated backing and 4,794 pooled sprites. Warmed near/pan/far frame p95 is **16.7–16.8 ms**, with an initial 33.3 ms sample. This authored 1,500-army fixture still applies normal visibility and does not measure a mature, fully visible empire or long-turn AI performance.
